# ===========================================
# This file is for LOCAL TESTING ONLY
# In Keboola, use component parameters instead
# ===========================================

# Anthropic API Key
ANTHROPIC_API_KEY=sk-ant-api03-...

# Model to use for job title mapping
# Supported models:
# - claude-sonnet-4-5 (recommended - fast, accurate, supports structured outputs)
# - claude-opus-4-1 (best quality, slower, more expensive, supports structured outputs)
MODEL=claude-sonnet-4-5

# Use structured outputs (beta feature)
# Only works with: claude-sonnet-4-5, claude-opus-4-1
USE_STRUCTURED_OUTPUT=true

# Batch size for processing job titles
BATCH_SIZE=100

# Limit number of records (0 = no limit)
# Use for testing before running on full dataset
LIMIT=1000
